<?xml version="1.0" encoding="utf-8"?>
<odoo>

 <!-- Categories -->
        <record id="view_event_category_form" model="ir.ui.view">
            <field name="name">Attendee tags</field>
            <field name="model">event.registration.category</field>
            <field name="arch" type="xml">
                <form string="Event Tag">
                    <group col="4">
                        <field name="name"/>
                        <field name="active"/>
                        <field name="parent_id"/>
                    </group>
                </form>
            </field>
        </record>
 		<record id="view_event_category_list" model="ir.ui.view">
            <field name="name">Attendee tags</field>
            <field name="model">event.registration.category</field>
            <field eval="6" name="priority"/>
            <field name="arch" type="xml">
                <tree string="Event Tags">
                    <field name="display_name"/>
                </tree>
            </field>
        </record>
        <record id="action_event_category_form" model="ir.actions.act_window">
            <field name="name">Attendee tags</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">event.registration.category</field>
            <field name="view_type">form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to create a new partner tag.
                </p>
                <p>
                    Manage partner tags to better classify them for tracking and analysis purposes.
                    A partner may have several categories and categories have a hierarchical structure: a partner with a category has also the parent category.
                </p>
            </field>
        </record>


<menuitem id="menu_event_category_form"
        action="action_event_category_form"
        name="Attendee Tags"
        sequence="1" parent="event.menu_event_configuration"
        groups="event.group_event_manager"/>
</odoo>
